Make it auto detect if a script is a srcMove xml file
Make it auto detect if a srcDiff has position information already.

Depending on these factors, adjust how the backend works to save calculation time.
